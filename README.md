# DeceptiveKeyInversion
Standard encryption assumes that a correct key produces plaintext and an incorrect key produces garbage. This paper describes a protocol that deliberately exploits that assumption as a security layer.
